#import the functions that you created from functions.py and use it in another file 
from functions  import square

print(square(10))


#When we built the web applications sometimes we re gonna need to use some other people's func 
#so that we can just import the functions instead of writing it by ourselves over again 
#Take advantage of existences features